import React, {useContext} from 'react'
import { Context } from '../index';

export const Navbar = () => {
    const {user}:any = useContext(Context);
    return (
        <div>
            
        </div>
    )
}
